name: Build Mobile Apps with EAS

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to build for'
        required: true
        type: choice
        options:
          - development
          - staging
          - production
        default: 'development'
      branch:
        description: 'Branch to build from'
        required: true
        type: string
        default: 'main'
      platform:
        description: 'Platform to build'
        required: true
        type: choice
        options:
          - android
          - ios
          - all
        default: 'all'

jobs:
  build-android:
    name: Build Android
    if: github.event.inputs.platform == 'android' || github.event.inputs.platform == 'all'
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout SalesX source code
        uses: actions/checkout@v4
        with:
          repository: 'Greatify/SalesX'
          ref: ${{ github.event.inputs.branch }}
          token: ${{ secrets.PAT_TOKEN }}
      
      - name: 📱 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: mobile/package-lock.json
      
      - name: 📦 Install dependencies
        working-directory: mobile
        run: npm ci --legacy-peer-deps
      
      - name: 🔧 Setup Expo and EAS
        run: |
          npm install -g eas-cli
          eas --version
      
      - name: 🚀 Build Android with EAS
        working-directory: mobile
        run: |
          eas build \
            --platform android \
            --profile ${{ github.event.inputs.environment }} \
            --non-interactive \
            --wait
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      
      - name: 📥 Download Android APK from EAS
        working-directory: mobile
        run: |
          # Get the latest build URL
          BUILD_URL=$(eas build:list --platform=android --limit=1 --json --non-interactive | jq -r '.[0].artifacts.buildUrl')
          echo "Downloading APK from: $BUILD_URL"
          
          # Download the APK
          curl -L -o SalesX-latest.apk "$BUILD_URL"
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      
      - name: 📤 Upload Android APK to S3
        working-directory: mobile
        run: |
          S3_KEY="mobile/android/release/SalesX-latest.apk"
          
          # Upload to S3 with proper headers for APK download
          aws s3 cp SalesX-latest.apk "s3://salesx-greatify/$S3_KEY" \
            --content-type "application/vnd.android.package-archive" \
            --content-disposition "attachment; filename=SalesX-latest.apk" \
            --metadata-directive REPLACE \
            --cache-control "no-cache, no-store, must-revalidate"
          
          # Generate CloudFront URL
          S3_CLOUDFRONT_URL="${{ secrets.S3_CLOUDFRONT_URL }}/$S3_KEY"
          echo "✅ Android APK uploaded to: $S3_CLOUDFRONT_URL"
          echo "ANDROID_URL=$S3_CLOUDFRONT_URL" >> $GITHUB_ENV
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.S3_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.S3_AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
      
      - name: 📋 Android Build Summary
        run: |
          echo "### ✅ Android Build Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Environment:** ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.event.inputs.branch }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Download APK:**" >> $GITHUB_STEP_SUMMARY
          echo "https://secure.klockwork.ai/mobile/android/release/SalesX-latest.apk" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Installation:**" >> $GITHUB_STEP_SUMMARY
          echo "1. Open the APK link on your Android device" >> $GITHUB_STEP_SUMMARY
          echo "2. Allow installation from unknown sources" >> $GITHUB_STEP_SUMMARY
          echo "3. Install the app" >> $GITHUB_STEP_SUMMARY
  
  build-ios:
    name: Build iOS
    if: github.event.inputs.platform == 'ios' || github.event.inputs.platform == 'all'
    runs-on: ubuntu-latest
    continue-on-error: true
    
    steps:
      - name: 📥 Checkout SalesX source code
        uses: actions/checkout@v4
        with:
          repository: 'Greatify/SalesX'
          ref: ${{ github.event.inputs.branch }}
          token: ${{ secrets.PAT_TOKEN }}
      
      - name: 📱 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: mobile/package-lock.json
      
      - name: 📦 Install dependencies
        working-directory: mobile
        run: npm ci --legacy-peer-deps
      
      - name: 🔧 Setup Expo and EAS
        run: |
          npm install -g eas-cli
          eas --version
      
      - name: 🚀 Build iOS with EAS
        working-directory: mobile
        run: |
          eas build \
            --platform ios \
            --profile ${{ github.event.inputs.environment }} \
            --non-interactive \
            --wait
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EXPO_APPLE_ID: ${{ secrets.EXPO_APPLE_ID }}
          EXPO_APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.EXPO_APPLE_APP_SPECIFIC_PASSWORD }}
      
      - name: 📥 Download iOS IPA from EAS
        working-directory: mobile
        run: |
          # Get the latest build URL
          BUILD_URL=$(eas build:list --platform=ios --limit=1 --json --non-interactive | jq -r '.[0].artifacts.buildUrl')
          echo "Downloading IPA from: $BUILD_URL"
          
          # Download and zip the IPA
          curl -L -o SalesX.ipa "$BUILD_URL"
          zip SalesX-latest.app.zip SalesX.ipa
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      
      - name: 📤 Upload iOS ZIP to S3
        working-directory: mobile
        run: |
          S3_KEY="mobile/ios/release/SalesX-latest.app.zip"
          ZIP_NAME="SalesX-latest.app.zip"
          
          # Upload to S3 with proper headers
          aws s3 cp "$ZIP_NAME" "s3://salesx-greatify/$S3_KEY" \
            --content-type "application/zip" \
            --content-disposition "attachment; filename=$ZIP_NAME" \
            --metadata-directive REPLACE \
            --cache-control "no-cache, no-store, must-revalidate"
          
          # Generate CloudFront URL
          S3_CLOUDFRONT_URL="${{ secrets.S3_CLOUDFRONT_URL }}/$S3_KEY"
          echo "✅ iOS ZIP uploaded to: $S3_CLOUDFRONT_URL"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.S3_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.S3_AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
      
      - name: 📋 iOS Build Summary
        run: |
          echo "### ✅ iOS Build Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Environment:** ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.event.inputs.branch }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Download IPA:**" >> $GITHUB_STEP_SUMMARY
          echo "https://secure.klockwork.ai/mobile/ios/release/SalesX-latest.app.zip" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Installation:**" >> $GITHUB_STEP_SUMMARY
          echo "1. Requires Apple Developer account" >> $GITHUB_STEP_SUMMARY
          echo "2. Install via TestFlight or OTA distribution" >> $GITHUB_STEP_SUMMARY
