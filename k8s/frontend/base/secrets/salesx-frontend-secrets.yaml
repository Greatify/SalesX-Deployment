apiVersion: v1
kind: Secret
metadata:
  name: salesx-frontend-secrets
  labels:
    app: salesx
    component: frontend
type: Opaque
stringData:
  # API Configuration
  api-url: "https://klockwork.ai/api"
  ws-url: "wss://klockwork.ai/api/ws"
  
  # Feature Flags (optional)
  enable-analytics: "true"
  enable-notifications: "true"
  
  # Environment
  environment: "production"
