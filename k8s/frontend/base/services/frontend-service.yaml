apiVersion: v1
kind: Service
metadata:
  name: salesx-frontend
  labels:
    app: salesx
    component: frontend
    tier: web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: salesx
    component: frontend
    tier: web
