apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  labels:
    app: salesx
    component: pgadmin
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: salesx
    component: pgadmin
